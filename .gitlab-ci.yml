image: docker:20.10.16

services:
  - docker:20.10.16-dind
  
stages:
  - app-build
  - test

job_build:
  stage: app-build
  script:
    - echo "Building the Spring Boot project"
    - mvn package

job_test:
  stage: test
  script:
    - echo "Running tests..."
    - mvn test
